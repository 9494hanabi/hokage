<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:versionCode="51"
    android:versionName="11.1">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="FTC Robot Controller"
        android:theme="@style/AppThemeRedRC">

        <!-- This is a stub manifest for the FtcRobotController module.
             The actual FtcRobotController app code is provided by the SDK AAR.
             This manifest provides the version info read by build.common.gradle. -->
    </application>
</manifest>
